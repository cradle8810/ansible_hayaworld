---
- name: Install i915 Driver
  ansible.builtin.import_tasks:
    file: tasks/igpu.yml

# TODO: Install From tarball
- name: Install Vulkan
  ansible.builtin.import_tasks:
    file: tasks/vulkan.yml
  when: igpu_vulkan_install
