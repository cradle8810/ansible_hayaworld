---
- name: "Install Docker"
  ansible.builtin.import_role:
    name: docker
  when: pihole_install_docker

- name: "Create Dirs"
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: '0755'
  loop:
    - "/root/pihole"
    - "/root/pihole/etc-dnsmasq.d"
    - "/root/pihole/etc-pihole"

- name: "Copy configs (Jinja template)"
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    owner: root
    group: root
    mode: '0644'
  loop:
    - src: "templates/root/pihole/docker-compose.yml.j2"
      dest: "/root/pihole/docker-compose.yml"
  notify: "Up pihole"

- name: "Copy configs (Files)"
  ansible.builtin.copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    owner: root
    group: root
    mode: '0644'
  loop:
    - src: "templates/root/pihole/etc-pihole/adlists.list"
      dest: "/root/pihole/etc-pihole/adlists.list"
  notify: "Up pihole"
