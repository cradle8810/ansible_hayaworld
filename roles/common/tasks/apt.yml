- name: "Common / Install Packages from apt"
  ansible.builtin.apt:
    update_cache: true
    pkg: "{{ common_pkg }}"
    cache_valid_time: "{{ common_apt.cache_valid_time }}"
  when: apt is defined
  tags:
    - apt

- name: "Common / Remove Packages"
  ansible.builtin.apt:
    pkg: "{{ common_pkg_remove }}"
    state: absent
    autoremove: true
    purge: true
  when: common_pkg_remove is defined
  tags:
    - apt
