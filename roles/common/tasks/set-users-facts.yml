---
- name: "Common / Set Hostinfo"
  ansible.builtin.set_fact:
    common_hostinfo: "{{ network | selectattr('ipv4', 'eq', common_mgmt_ipv4) | first }}"
  vars:
    common_mgmt_ipv4: "{{ ansible_all_ipv4_addresses | select('match', '192.168.88.') | first }}"
  tags:
    - always
