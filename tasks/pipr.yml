---
- name: Download pipr (Linux)
  ansible.builtin.get_url:
    url: "{{ pipr.url }}"
    checksum: "{{ pipr.checksum }}"
    dest: /opt/bin/pipr
    mode: "0755"
  when: ansible_distribution == "Ubuntu"
