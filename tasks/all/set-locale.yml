---
- name: "Install locale en_US"
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop:
    - locales-all
    - language-pack-en

- name: "Set locale as en_US.UTF-8"
  community.general.locale_gen:
    name: "en_US.UTF-8"
    state: present
